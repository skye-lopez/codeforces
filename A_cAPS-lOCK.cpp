#include <algorithm>
#include <bits/stdc++.h>
#include <string>
#include <cctype>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    // if str[i] is lower and str[i+1-n] are upper yes ["z"] counts too
    // if str[i-n] are all upper
    string str; cin >> str;
    char a = 'a';
    lower = tolower(a)
    for(int i=0; i<str.size(); ++i) {
    }
}
